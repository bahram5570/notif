import { SingleDateTypes } from '../../../__hooks__/useCalendarGetData/types';

export type UseCalendarDayInfoProps = SingleDateTypes;

export type CalendarDayInfoTypes = {
  backgroundColor: string;
  birthIconColor: string;
  isGiveBirth: boolean;
  dayTypeColor: string;
  isBirthday: boolean;
  dayColor: string;
  dayTitle: string;
};
