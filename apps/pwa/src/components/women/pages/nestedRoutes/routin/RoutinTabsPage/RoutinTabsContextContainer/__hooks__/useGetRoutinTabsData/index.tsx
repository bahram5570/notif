import { UseGetRoutinTabsDataPropsType } from './type';

const useGetRoutinTabsData = ({ activeTab }: UseGetRoutinTabsDataPropsType) => {
  const data = null;
  return { data };
};

export default useGetRoutinTabsData;
