export type SupportInfoSubmitType = {
  packageName: string;
  version: string;
  type: number;
  modelPhone: string;
  text: string;
};

export type ResponseType = {
  result: string;
  valid: boolean;
};
