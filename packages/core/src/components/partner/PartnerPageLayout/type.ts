export type PartnerPageLayoutPropsType = {
  children: React.ReactNode;
  isValid: boolean;
  coverImage: string;
  defaultCoverImage: string;
};

export type ProfileButtonPropsType = {
  coverImage: string;
};
